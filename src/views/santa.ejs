<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>NodeJS Santa App</title>
    <!-- Linking custom stylesheet -->
    <link rel="stylesheet" href="/css/styles.css" />
    <!-- Linking Bootstrap CSS for styling -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <!-- Meta description for SEO -->
    <meta
      name="description"
      content="Submit your wish to Santa using the Santa App."
    />
    <!-- Adding festive styles -->
    <style>
      body {
        background-color: #f0f8ff; /* Light background color */
        font-family: "Arial", sans-serif; /* Set a fun font */
        color: #333; /* Darker text color for better readability */
      }
      .container {
        background: url("path/to/your/santa-background.jpg") no-repeat center
          center; /* Background image */
        background-size: cover;
        border-radius: 15px; /* Rounded corners */
        padding: 30px; /* Internal padding */
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.2); /* Subtle shadow */
      }
      h1 {
        color: #ff6347; /* Tomato color for the title */
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3); /* Add shadow for depth */
      }
      p {
        color: #444; /* Darker gray for text */
      }
      button {
        background-color: #28a745; /* Bootstrap primary color */
        transition: background-color 0.3s ease; /* Smooth transition */
      }
      button:hover {
        background-color: #218838; /* Darker green on hover */
      }
      .form-control,
      .form-control:focus {
        border: 2px solid #ff6347; /* Tomato color border */
        box-shadow: none; /* Remove default shadow */
      }
      .form-control::placeholder {
        color: #ccc; /* Light placeholder text */
      }
      .form-group label {
        font-weight: bold; /* Make labels bold */
      }
    </style>
  </head>
  <body>
    <div class="container mt-5">
      <h1 class="text-center">🎅 Hello User, Welcome!</h1>
      <p class="text-center">What do you want for Christmas from Santa?</p>

      <!-- Santa wish submission form -->
      <form method="post" class="mx-auto" style="max-width: 600px">
        <div class="form-group mb-3">
          <label for="username" class="form-label">Your User ID:</label>
          <input
            type="text"
            id="username"
            name="username"
            class="form-control"
            placeholder="Enter your User ID"
            required
          />
        </div>

        <div class="form-group mb-3">
          <label for="wish" class="form-label">Your Christmas Wish:</label>
          <textarea
            id="wish"
            name="wish"
            class="form-control"
            rows="5"
            maxlength="100"
            placeholder="Enter your wish"
            required
          ></textarea>
        </div>

        <button
          type="submit"
          id="submit-letter"
          class="btn btn-primary btn-block"
        >
          Send to Santa
        </button>
      </form>
    </div>

    <!-- Bootstrap JS and dependencies (optional) -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-MrcW6ZMFYhlYjl6eFaBOdBG9KR2Y5fKnQz5htPhOy2u6p17s4I3ncpNhPsoEvJ2"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
